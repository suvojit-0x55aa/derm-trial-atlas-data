__pycache__/
*.pyc
.venv/
.env
.omc/

# Bulk, reproducible output of scripts/fetch_condition_landscape.py -- not
# curated data, regenerate locally instead of committing (see
# docs/condition-landscape-quickstart.md). Can include large PDFs when run
# with --fetch-protocol-pdfs.
data/condition_landscape/
