# schema/trial.schema.json

Static snapshot of the schema v2 JSON Schema (draft-07). Generated from
`atlas/schema.py` by `scripts/export_schema.py` in `kolai-website`, which
owns both files now that this repo is data-only. Won't auto-update here --
refreshed from kolai-website when the schema changes. See `../docs/SCHEMA.md`
for the human-readable field reference.
